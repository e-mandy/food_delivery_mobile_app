import 'package:flutter/material.dart';

class CategoryPizza extends StatelessWidget {
  const CategoryPizza({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}